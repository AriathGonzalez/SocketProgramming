import React from "react";
// Note: Use toasts from React Bootstrap for the messages.
// Note: Also, possibly look into offcanvas from React Bootstrap.
// Note: Also, possibly look into badges for message counter.

export default function Chat() {
  return <div>Chat</div>;
}
